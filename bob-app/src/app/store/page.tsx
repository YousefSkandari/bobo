import ClientStorePage from '@/components/store/ClientStorePage';

export const metadata = {
  title: 'BOB | Cosmetic Store',
  description: 'Customize your city with unique building styles and decorations',
};

export default function StorePage() {
  return <ClientStorePage />;
} 